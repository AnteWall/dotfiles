---
- name: Bootstrap local machine
  hosts: localhost
  gather_facts: true
  become: false
  vars_files:
    - ../group_vars/all.yml

  roles:
    - role: common
    - role: linux
      when: ansible_facts['system'] == "Linux"
    - role: macos
      when: ansible_facts['system'] == "Darwin"
